import { ReservedenquiryPipe } from './reservedenquiry.pipe';

describe('ReservedenquiryPipe', () => {
  it('create an instance', () => {
    const pipe = new ReservedenquiryPipe();
    expect(pipe).toBeTruthy();
  });
});
