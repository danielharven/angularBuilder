version: '3.2'
services:
  portal:
    image: 'nginx:latest'
    ports:
      - 80:80
    volumes:
      - ./dist:/usr/share/nginx/html
    container_name: nginx
    environment:
      - NGINX_HOST=portal.e-sapp.gov.zm
